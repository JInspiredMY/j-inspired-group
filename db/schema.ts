// Drizzle schema for the Phase 1 platform build (JI-PMO-001 v1.8).
//
// This file is intentionally empty at the infra-scaffolding stage — it exists
// so `drizzle.config.ts` has a valid entry point and `drizzle-kit generate`
// works end-to-end before any table is defined.
//
// R1 (Destination Registry) adds the first real table here in a follow-up
// change — see docs/journeyinspired-group_design-brief_v1.4.md Appendix A.
export {}
