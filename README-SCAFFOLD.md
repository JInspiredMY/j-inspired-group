Scaffold notes

Files added as a minimal Next.js App Router scaffold. Next steps:

1. Run `npm install` to install dependencies.
2. Run `npm run dev` to start the dev server.
3. Wire `EnquiryForm` to a server-side API route that calls Resend.
4. Replace placeholder tokens in `styles/tokens.css` with brand values when available.
